pipeline {
    agent any

    stages {
        
        stage('Verify if file exists') {
            steps {
                fileExists '/build/build.sh'
            }
        }
        
        stage('Build') {
            steps {
              sh '/build/build.sh'
            }
        }
    }
}

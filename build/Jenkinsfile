pipeline {
    agent any

    stages {
        
        stage('Verify if file exists') {
            steps {
                fileExists '/build/build.sh'
            }
        }
        
        stage('Build') {
            steps {
              sh 'chmod +x build/build.sh'
              sh 'docker build -t -f build/dockerfile
            }
        }
    }
}
